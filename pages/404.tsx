import { ErrorView } from '@components';
import { Fragment } from 'react';

const NotFoundPage = () => {
  return (
    <Fragment>
      <ErrorView />;
    </Fragment>
  );
};

export default NotFoundPage;
